{
 "id": "https://ns.cisecurity.org/benchmark/ubuntu-24.04-lts/req/cisb-ubnt24.04lts-002.001.019_ctrl",
 "version": "1.0.0",
 "label": "2.1.19",
 "lifecycle": "active",
 "title": "Ensure xinetd services are not in use",
 "statements": [
  {
   "id": "s1",
   "modality": "unspecified",
   "obligated-parties": [
    "https://ns.cisecurity.org/benchmark/ubuntu-24.04-lts/party/system-administrator"
   ],
   "prose": {
    "en": "Ensure xinetd services are not in use"
   }
  }
 ],
 "aliases": [
  {
   "scheme": "xccdf",
   "value": "xccdf_org.cisecurity.benchmarks_rule_2.1.19_Ensure_xinetd_services_are_not_in_use"
  }
 ],
 "facets": {
  "https://ns.cisecurity.org/benchmark/ubuntu-24.04-lts/facet/narrative@1": {
   "description": {
    "en": "The eXtended InterNET Daemon (`xinetd`) is an open source super daemon that replaced the original `inetd` daemon. The `xinetd` daemon listens for well known services and dispatches the appropriate daemon to properly respond to service requests."
   },
   "rationale": {
    "en": "If there are no `xinetd` services required, it is recommended that the package be removed to reduce the attack surface are of the system.\n\n**Note:** If an `xinetd` service or services are required, ensure that any `xinetd` service not required is stopped and masked"
   }
  },
  "https://ns.oscal.org/stdlib/facet/assessment-criteria@1": {
   "method": "automated",
   "audit": {
    "en": "Run the following commands to stop `xinetd.service`, and remove the `xinetd` package:  \n\n**\\-OR\\-**  \n\n**\\-IF\\-** the `xinetd` package is required as a dependency:  \n\nRun the following commands to stop and mask the `xinetd.service`:  \n\nImpact:  \n\nThere may be packages that are dependent on the `xinetd` package. If the `xinetd` package is removed, these dependent packages will be removed as well. Before removing the `xinetd` package, review any dependent packages to determine if they are required on the system.  \n\n**\\-IF\\-** a dependent package is required: stop and mask xinetd.service leaving the xinetd package installed.\n\n```\n# systemctl stop xinetd.service\n# systemctl mask xinetd.service\n```\n\n```\n# systemctl stop xinetd.service\n# apt purge xinetd\n```"
   },
   "remediation": {
    "en": "Run the following commands to stop `xinetd.service`, and remove the `xinetd` package:  \n\n**\\-OR\\-**  \n\n**\\-IF\\-** the `xinetd` package is required as a dependency:  \n\nRun the following commands to stop and mask the `xinetd.service`:  \n\nImpact:  \n\nThere may be packages that are dependent on the `xinetd` package. If the `xinetd` package is removed, these dependent packages will be removed as well. Before removing the `xinetd` package, review any dependent packages to determine if they are required on the system.  \n\n**\\-IF\\-** a dependent package is required: stop and mask xinetd.service leaving the xinetd package installed.\n\n```\n# systemctl stop xinetd.service\n# systemctl mask xinetd.service\n```\n\n```\n# systemctl stop xinetd.service\n# apt purge xinetd\n```"
   }
  },
  "https://ns.cisecurity.org/benchmark/ubuntu-24.04-lts/facet/external-references@1": {
   "references": [
    "NIST SP 800-53 Rev. 5: CM-7"
   ]
  }
 }
}
