{
  "id": "https://ns.oscal.org/stdlib/facet/effectivity",
  "version": "1.0.0",
  "modifies-semantics": ["selection"],
  "note": "NORMATIVE (gate item 2). Declaration promoted [] -> [selection] per D10 rev 2 (backlog #8): time-gated applicability is selection by calendar - a selecting tool that ignores effectivity windows selects wrongly. Window values carry the source's own shape (CR26 obtain/maintain/grace machinery observed); kernel absorption (lifecycle + L0 versioning cover part of the need) stays a D22-rev audit question.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object", "minProperties": 1,
    "additionalProperties": {
      "type": "object",
      "properties": {
        "is": { "type": "string" },
        "current_status": { "type": "string" },
        "date": { "type": "object" }
      }
    }
  }
}
