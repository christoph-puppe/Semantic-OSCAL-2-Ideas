{
  "id": "https://ns.oscal.org/stdlib/facet/reporting-obligation",
  "version": "1.0.0",
  "modifies-semantics": ["assessment"],
  "note": "NORMATIVE (gate item 2). Declaration promoted [] -> [assessment] per D10 rev 2 (backlog #8): notification duties are checkable - they alter what passing means, and an assessing tool that cannot read them must halt. Also the strongest anticipated-convergence candidate after terminology (D22 rev audit): NIS2 / DORA / CRA are notification-duty regimes; kernel promotion revisits when an EU corpus lands.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object", "additionalProperties": false,
    "properties": {
      "notification": {
        "type": "array", "minItems": 1,
        "items": {
          "type": "object", "additionalProperties": false,
          "properties": {
            "party": { "type": "string" },
            "method": { "type": "string" },
            "target": { "type": "string" },
            "name": { "type": "object", "additionalProperties": { "type": "string" } }
          }
        }
      },
      "following-information": { "type": "array" },
      "following-information-bullets": { "type": "array" }
    }
  }
}
